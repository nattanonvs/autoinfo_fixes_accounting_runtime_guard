<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="sale_quotation_tree" model="ir.ui.view">
        <field name="name">sale.quotation.tree</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree"/>
        <field name="arch" type="xml">
            <field name="state" position="after">
                <field name="scheduled_date" widget="remaining_days" optional="hide"/>
            </field>
        </field>
    </record>

    <record id="sale_order_tree" model="ir.ui.view">
        <field name="name">sale.order.tree</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree"/>
        <field name="arch" type="xml">
            <field name="state" position="after">
                <field name="scheduled_date" widget="remaining_days" optional="hide"/>
            </field>
        </field>
    </record>

    <record id="sale_order_search" model="ir.ui.view">
        <field name="name">sale.order.search</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter" />
        <field name="arch" type="xml">
            <filter name="my_sale_orders_filter" position="after">
                <filter name="overdue" string="Overdue" domain="[('scheduled_date', '&lt;', time.strftime('%Y-%m-%d %H:%M:%S')), ('is_all_delivered', '=', False)]"/>
            </filter>
        </field>
    </record>

    <record id="sale_order_form" model="ir.ui.view">
        <field name="name">sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="%(action_change_delivery_date)d" type="action" string="Change Delivery Date" attrs="{'invisible': [('state', 'in', ['draft', 'cancel'])]}" context="{'default_sale_order_id': id}"/>
            </xpath>
            <field name="commitment_date" position="attributes">
                <attribute name="attrs">{'readonly': [('state', '!=', 'draft')]}</attribute>
            </field>
            <field name="source_id" position="after">
                <field name="reason_change_delivery_date"/>
            </field>
        </field>
    </record>
</odoo>