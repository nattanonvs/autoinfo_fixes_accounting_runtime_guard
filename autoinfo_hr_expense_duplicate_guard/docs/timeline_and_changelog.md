# Timeline And Changelog

## 15.0.1.0.0
- Delivered the initial HR expense duplicate guard module.
- Added duplicate fields and rule execution on `hr.expense`.
- Added duplicate-hit evidence model `hr.expense.duplicate.hit`.
- Added submit and approval guards on `hr.expense.sheet`.
- Added override wizard with mandatory reason capture.
- Added security group `Expense Duplicate Override` and access rules.
- Added expense and expense-sheet UI for duplicate review.
- Added automated tests covering duplicate logic, overrides, and UI injections.
- Added module documentation in `README.md` and `docs/`.
